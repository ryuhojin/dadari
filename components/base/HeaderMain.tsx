import styled from "styled-components";

const StyledHeaderMain = styled.header`
  background: #fff;
  height: 60px;
`;

const HeaderMain = () => {
  return <StyledHeaderMain></StyledHeaderMain>;
};

export default HeaderMain;
