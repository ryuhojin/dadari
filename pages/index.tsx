import MainTemplate from "components/template/MainTemplate";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return <MainTemplate>
    
  </MainTemplate>;
};

export default Home;
