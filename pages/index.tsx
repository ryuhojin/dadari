import MainTemplate from "components/layouts/MainTemplate";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return <MainTemplate>
    
  </MainTemplate>;
};

export default Home;
